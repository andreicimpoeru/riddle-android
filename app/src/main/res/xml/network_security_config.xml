<?xml version="1.0" encoding="utf-8"?>
<!-- Self-hosted servers are often plain HTTP on a LAN IP. HTTPS is still preferred. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>